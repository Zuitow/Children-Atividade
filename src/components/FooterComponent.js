import { Text, View, StyleSheet } from "react-native";
import { estilos } from "../styles/StylesHome";

export default function FooterComponent() {
  return (
    <View style={{height: '100%', height: 100, backgroundColor: 'gray', }}>
    <Text>Eita meu fi</Text>
    </View>
  )
}
